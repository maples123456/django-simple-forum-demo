services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: forum
      POSTGRES_USER: forum
      POSTGRES_PASSWORD: change-me
    ports:
      - "5432:5432"
    volumes:
      - forum_postgres_data:/var/lib/postgresql/data

volumes:
  forum_postgres_data:
